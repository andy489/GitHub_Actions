name: Deploy website
on: 
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          cd events-deep-dive
          npm ci
      - name: Lint code
        run: |
          cd events-deep-dive
          npm run lint 
      # - name: Install npm
      #   run: |
      #     cd events-deep-dive
      #     npm install 
      - name: Test code
        run: |
          cd events-deep-dive
          npm install
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          cd events-deep-dive
          npm ci
      - name: Build website
        run: |
          cd events-deep-dive
          npm run build
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying..."

        